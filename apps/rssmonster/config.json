{
  "name": "RSSMonster",
  "id": "rssmonster",
  "available": true,
  "short_desc": "Self-hosted RSS reader with smart folders and search",
  "author": "pietheinstrengholt",
  "port": 8431,
  "categories": [
    "utilities"
  ],
  "description": "RSSMonster is a modern, multi-user RSS reader with smart folders, full-text search, a three-pane reading layout and Fever / Google Reader API compatibility.\n\nThis definition uses the MySQL profile (parallel crawling and AI jobs) with a crawl worker, an AI worker and the inference service. Every AI capability (embeddings, summaries/tags, scoring, assistant) goes to one OpenAI-compatible endpoint such as a LiteLLM gateway: no model is downloaded or run locally.",
  "tipi_version": 6,
  "version": "latest",
  "source": "https://github.com/pietheinstrengholt/rssmonster",
  "exposable": true,
  "supported_architectures": [
    "arm64",
    "amd64"
  ],
  "created_at": 1789250400000,
  "updated_at": 1790023520268,
  "dynamic_config": true,
  "form_fields": [
    {
      "type": "random",
      "label": "JWT secret",
      "min": 64,
      "env_variable": "RSSMONSTER_JWT_SECRET"
    },
    {
      "type": "random",
      "label": "Fever credential secret",
      "hint": "Also derives the key encrypting stored inference API keys. Changing it invalidates them.",
      "min": 64,
      "env_variable": "RSSMONSTER_FEVER_CREDENTIAL_SECRET"
    },
    {
      "type": "random",
      "label": "MySQL password",
      "min": 32,
      "env_variable": "RSSMONSTER_MYSQL_PASSWORD"
    },
    {
      "type": "random",
      "label": "MySQL root password",
      "min": 32,
      "env_variable": "RSSMONSTER_MYSQL_ROOT_PASSWORD"
    },
    {
      "type": "number",
      "label": "Feed crawl concurrency",
      "hint": "Feeds fetched simultaneously by the crawl worker.",
      "default": 3,
      "required": false,
      "env_variable": "RSSMONSTER_FEED_CONCURRENCY"
    },
    {
      "type": "number",
      "label": "AI job concurrency",
      "hint": "AI jobs (summaries, tags, embeddings) run simultaneously. Bounded by what the LLM endpoint can serve in parallel.",
      "default": 2,
      "required": false,
      "env_variable": "RSSMONSTER_AI_JOB_CONCURRENCY"
    },
    {
      "type": "boolean",
      "label": "Allow registration",
      "hint": "The first registered account becomes admin. Disable once your accounts exist, before exposing the instance.",
      "default": true,
      "required": false,
      "env_variable": "RSSMONSTER_ALLOW_REGISTRATION"
    },
    {
      "type": "boolean",
      "label": "Enable AI",
      "hint": "Summaries, tags, scores, semantic search and assistant through the endpoint below.",
      "default": false,
      "required": false,
      "env_variable": "RSSMONSTER_AI_ENABLED"
    },
    {
      "type": "url",
      "label": "OpenAI-compatible base URL",
      "hint": "Must end with /v1, e.g. https://litellm.example.com/v1",
      "required": false,
      "env_variable": "RSSMONSTER_LLM_BASE_URL"
    },
    {
      "type": "password",
      "label": "API key",
      "required": false,
      "env_variable": "RSSMONSTER_LLM_API_KEY"
    },
    {
      "type": "text",
      "label": "Embedding model",
      "hint": "Definitive for the database: vectors from different models cannot be mixed and are not migrated.",
      "default": "qwen3-embedding-8b",
      "required": false,
      "env_variable": "RSSMONSTER_EMBEDDING_MODEL"
    },
    {
      "type": "number",
      "label": "Embedding dimensions",
      "hint": "Must match the model output (qwen3-embedding-8b: 4096).",
      "default": 4096,
      "required": false,
      "env_variable": "RSSMONSTER_EMBEDDING_DIMENSIONS"
    },
    {
      "type": "text",
      "label": "Generation model",
      "hint": "Summaries, tags, labels and article scoring.",
      "default": "qwen",
      "required": false,
      "env_variable": "RSSMONSTER_GENERATION_MODEL"
    },
    {
      "type": "text",
      "label": "Assistant model",
      "hint": "Needs tool calling and streaming.",
      "default": "qwen",
      "required": false,
      "env_variable": "RSSMONSTER_ASSISTANT_MODEL"
    },
    {
      "type": "text",
      "label": "Summary language",
      "hint": "Language of the AI bullet summaries, e.g. French. Empty keeps upstream behaviour (usually English). Existing summaries are not regenerated.",
      "required": false,
      "env_variable": "RSSMONSTER_SUMMARY_LANGUAGE"
    }
  ]
}